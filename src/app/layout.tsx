import type { Metadata, Viewport } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "BagiStruk — Smart Receipt Splitter & Finance Tracker",
  description:
    "Scan struk belanja, bagi tagihan dengan mudah, dan lacak pengeluaranmu secara otomatis. Simple. Smart. Split.",
  keywords: ["receipt scanner", "split bill", "finance tracker", "struk", "bagi tagihan"],
  authors: [{ name: "BagiStruk" }],
};

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
  themeColor: "#109981",
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="id"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
